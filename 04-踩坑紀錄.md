---
type: note
project: personal-website
created: 2026-07-12
updated: 2026-07-18
status: seedling
---

# 個人網站重建 — 踩坑紀錄

## GitHub CLI OAuth Token 失效導致 workflow 權限不足
**日期**：2026-07-12
**環境**：macOS + gh CLI + GitHub Actions

### 問題描述
執行 `git push` 時回傳 `refusing to allow an OAuth App to create or update workflow` 錯誤。`gh auth status` 顯示「The token in keyring is invalid」。

### 排查過程
1. 執行 `gh auth refresh -h github.com -s workflow` — SSL 憑證錯誤（`x509: OSStatus -26276`）
2. 檢查 GitHub Settings → Applications → GitHub CLI — 權限正確（含 workflow scope）
3. 判斷是本地 keyring 中的 token 過期/失效

### 根因
gh CLI 儲存在 macOS Keychain 的 OAuth token 可能因以下原因失效：
- Token 過期
- 權限變更後未同步
- 網路環境問題（proxy/SSL inspection）

### 解法
1. 開啟 GitHub 裝置授權頁面：https://github.com/login/device
2. 執行 `gh auth refresh -h github.com -s repo,workflow` 取得 8 位數授權碼
3. 在瀏覽器輸入授權碼完成認證
4. 認證完成後 `git push` 成功

```bash
# 若 SSL 錯誤持續，嘗試關閉 sandbox 或直接用 git push 觸發瀏覽器授權
gh auth refresh -h github.com -s repo,workflow
git push origin main
```

### 教訓
- gh CLI token 失效時，`gh auth status` 會明確顯示「token in keyring is invalid」
- `gh auth refresh` 可能因 SSL 問題失敗（公司網路/proxy），此時可直接用 `git push` 觸發瀏覽器授權
- GitHub Settings → Applications 可查看已授權應用的權限
- workflow scope 是獨立於 repo scope 的，需要明確指定

---

# 個人網站重建 — 踩坑紀錄

## RSS XML CDATA 標籤解析失敗
**日期**：2026-07-11
**環境**：`scripts/rss-aggregate.mjs`（Node.js 22）

### 問題描述
RSS feed 的 `<title>` 或 `<description>` 欄位常包裹在 `<![CDATA[...]]>` 中，直接用 regex 匹配會把 CDATA 標籤也抓進去，導致產出的 Markdown 標題含有 `<![CDATA[` 前綴。

### 排查過程
1. 先嘗試用 `textContent` 提取 — 但自寫 XML 解析器沒有 DOM API
2. 發現是 CDATA 區段未被剝離

### 根因
RSS 2.0 規範允許用 CDATA 包裹含特殊字元的文字內容；自寫 regex 解析器未處理此案例。

### 解法
在 XML 解析函式中加入 CDATA stripping 步驟：

```javascript
// 先提取 CDATA 內容，再移除 CDATA 標籤
function stripCDATA(text) {
  return text.replace(/<!\[CDATA\[(.*?)\]\]>/gs, '$1');
}
```

### 教訓
- 自寫 XML 解析器必須處理 CDATA、entity encoding（`&amp;`、`&lt;`）等邊界案例
- 若 RSS 源格式不統一，先用最寬鬆的 regex 再逐步收緊

---

## 中文標題 slugify 產生空字串
**日期**：2026-07-11
**環境**：`scripts/rss-aggregate.mjs`

### 問題描述
將中文標題轉為 URL-safe slug 時，原本用 `title.replace(/[^\w\s-]/g, '')` 過濾特殊字元，但 `\w` 在 JavaScript 只匹配 `[a-zA-Z0-9_]`，不包含中文字。結果中文標題被清空，所有文章 slug 都變成空字串。

### 排查過程
1. 觀察到所有中文文章產生的 slug 都是 `-` 或空字串
2. 檢查 regex 發現 `\w` 不含中文

### 根因
JavaScript `\w` 不等於 Unicode word character；中文字需要額外範圍。

### 解法
改用包含 CJK Unified Ideographs 範圍的 regex：

```javascript
function slugify(title) {
  return title
    .toLowerCase()
    .replace(/[^\w一-鿿\s-]/g, '')  // 保留中文、英文、數字、空格、連字號
    .replace(/[\s_]+/g, '-')
    .replace(/^-+|-+$/g, '')
    .slice(0, 60);
}
```

### 教訓
- JavaScript regex 的 `\w` 只管 ASCII，處理多語言需手動加 Unicode range
- CJK Unified Ideographs 基本區塊：`U+4E00` ~ `U+9FFF`（即 `一-鿿`）

---

## Reddit RSS 回傳 403 Forbidden
**日期**：2026-07-11
**環境**：GitHub Actions runner（Ubuntu）

### 問題描述
`scripts/rss-aggregate.mjs` 抓取 Reddit RSS（`reddit.com/.rss`）時回傳 HTTP 403，本機測試偶爾也會失敗。

### 排查過程
1. 嘗試加 User-Agent header — 仍 403
2. 嘗試不同 Reddit 子版 RSS URL — 全部 403
3. 判斷 Reddit 對伺服器端 IP 進行封鎖

### 根因
Reddit 逐步限制伺服器端 RSS 存取，GitHub Actions runner IP 被標記為 bot。

### 解法
改用 RSSHub 提供的 36Kr 源作為替代，成功抓取：

```json
{
  "name": "36Kr",
  "url": "https://rsshub.app/36kr/newsflashes",
  "category": "tech"
}
```

### 教訓
- 大型平台（Reddit、Twitter）逐漸封鎖無認證 RSS
- RSSHub 是好替代方案，但需注意公共實例的穩定性
- 配置檔 `rss-feeds.json` 設計為可替換源，方便未來調整

---

## Cloudflare API Token 缺少 Zone DNS Edit 權限
**日期**：2026-07-11
**環境**：Cloudflare Dashboard + Pages 部署

### 問題描述
嘗試透過 Cloudflare API 為 Pages 專案建立自訂域名 DNS 記錄時，API 回傳權限不足錯誤。

### 排查過程
1. 確認 Token 可成功呼叫 Pages API
2. 呼叫 DNS API 時回傳 `Authentication error`
3. 檢查 Token 權限發現只有 Workers Scripts Edit，缺少 Zone DNS Edit

### 根因
Cloudflare API Token 最小權限原則：不同資源需要不同 permission scope。

### 解法
到 Cloudflare Dashboard → My Profile → API Tokens → 編輯 Token → 補齊：
- Zone → DNS → Edit
- Zone → Zone → Read

### 教訓
- Cloudflare Token 權限要根據操作範圍分開設定
- Workers 部署 ≠ DNS 管理，兩者需要各自 permission
- 記錄在 [[Cloudflare API 與 Wrangler 紀錄]] 的最小權限建議中

---

## Guardrail 擋 rm 指令
**日期**：2026-07-11
**環境**：Claude Code sandbox + guardrail

### 問題描述
在清理 RSS 快取或測試檔案時，執行 `rm` 指令被 guardrail 拒絕。

### 根因
全域 guardrail（`~/Documents/hooks/shared-guardrail.py`）禁止 `rm` 指令以保護檔案安全。

### 解法
改用 `trash` 指令（macOS trash utility），功能等同移至回收桶。

### 教訓
- 在這個開發環境中，`rm` 一律用 `trash` 取代
- 這是 guardrail 的設計意圖，不是 bug

---

## TradingView widget async 載入失敗
**日期**：2026-07-12
**環境**：Astro 7 + `src/components/TradingChart.astro`（Islands 架構）

### 問題描述
將 TradingView Widget 元件放到 Astro Island 元件中時，TradingView 的非同步初始化 (`new TradingView.widget(...)`) 無法正常執行。常見錯誤為：
- `TradingView is not defined` — script 尚未載入完成
- 或 widget 初始化後畫面空白，console 無錯誤

### 排查過程
1. 嘗試 `client:load` directive — 仍不穩定
2. 嘗試在 `onMounted` 中延遲加載 — 交替成功/失敗
3. 判斷問題出在 Astro 的 JS 執行環境與 TradingView widget 的非同步初始化的交互

### 根因
Astro Islands 架構使用 partial hydration，與 TradingView widget 依賴的 `document.write` 和 DOM 操作方式衝突。TradingView 的 script 需要完整的頁面載入生命週期，而 Astro Island 的執行環境經過框架包裝。

### 解法
改用獨立 iframe embed 方案：
1. 建立 `src/pages/trading-view-embed.html` — 一個純 HTML 頁面，無框架干擾
2. 在 `index.astro` 中用 `<iframe src="/trading-view-embed.html">` 嵌入
3. iframe 完全隔離 JavaScript 執行環境，TradingView widget 正常初始化

```html
<!-- src/pages/trading-view-embed.html -->
<!DOCTYPE html>
<html>
<head>
  <script src="https://s3.tradingview.com/tv.js"></script>
</head>
<body>
  <div id="tv-chart"></div>
  <script>
    new TradingView.widget({
      container_id: "tv-chart",
      symbol: "BITSTAMP:BTCUSD",
      // ... 其餘 TradingView 設定
    });
  </script>
</body>
</html>
```

### 教訓
- Astro Islands 的 partial hydration 與第三方 widget 庫（特別是依賴 `document.write` 或完整頁面生命週期的）容易衝突
- iframe embed 不是最優雅的方案，但隔離效果最好，適合裝載不可控的第三方內容
- 撰寫自訂元件（Lightweight Charts）可搭配 client:load directive，不受此限制

---

## RSS 中文標題 slug 路徑衝突
**日期**：2026-07-12
**環境**：`scripts/rss-aggregate.mjs` + `articles/[slug].astro`

### 問題描述
RSS 聚合腳本將文章寫入 `src/content/posts/rss/` 目錄，slug 取自檔名 `rss/中文標題.md`。動態路由 `articles/[slug].astro` 使用 `post.id` 作為 slug，但 `post.id` 回傳完整路徑 `rss/中文標題`，導致：
- 路由匹配失敗（slug 包含 `rss/` 前綴）
- 或連結產生錯誤的 URL 路徑

### 排查過程
1. 觀察到 RSS 文章的 URL 是 `/articles/rss/中文標題` 而非 `/articles/中文標題`
2. 檢查 `post.id` 發現包含集合內的子路徑 `rss/`
3. 確認動態路由取 slug 時需萃取最後一段

### 根因
Astro Content Collections 的 `post.id` 是相對於集合目錄的完整路徑。當文章存放在子目錄（`rss/` 或 `gmail/`）時，`post.id` 包含該路徑前綴。

### 解法
在動態路由頁面使用 `post.id.split('/').pop()` 萃取最後一段作為 slug：

```javascript
// src/pages/articles/[slug].astro 中取出 slug
export async function getStaticPaths() {
  const posts = await getCollection('posts');
  return posts.map(post => ({
    params: { slug: post.id.split('/').pop() },
    props: { post },
  }));
}
```

### 教訓
- Astro v7 的 `post.id` 是相對於集合資料夾的完整路徑，非純檔名
- 巢狀目錄結構的文章（RSS、Gmail）需萃取最後一段
- 建議在 slugify 階段就確保檔名是唯一的 URL-safe 名稱

---

## GitHub Actions workflow push 權限不足
**日期**：2026-07-12
**環境**：`.github/workflows/rss-aggregate.yml`（GitHub Actions runner）

### 問題描述
RSS 聚合 workflow 執行成功，但在 auto-commit 階段（`git push`）回傳 `refusing to allow an OAuth App to create or update workflow` 錯誤。Workflow 無法自動 commit 產生的 Markdown 檔案。

### 排查過程
1. 確認 `git config` 的使用者名稱與 email 正確
2. 檢查 `git push` 錯誤訊息指向 OAuth scope 限制
3. 發現預設 `GITHUB_TOKEN` 沒有 `workflow` scope

### 根因
GitHub Actions 中，由 `GITHUB_TOKEN` 執行的 git 操作預設不允許修改 `.github/workflows/` 目錄（即使 RSS workflow 想 commit 的不是 workflow 檔案本身，某些情況下仍會受 scope 影響）。若 workflow 需要自行 commit 並 push，需補足 `workflow` scope。

### 解法
使用 `gh auth refresh` 補足 workflow scope：

```bash
# 在本機或 workflow step 中執行
gh auth refresh -s workflow
```

或在 workflow YAML 中明確設定 permissions：

```yaml
# .github/workflows/rss-aggregate.yml
jobs:
  aggregate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      workflows: write  # 補足 workflow push 權限
```

### 教訓
- GitHub Actions 預設 GITHUB_TOKEN 的 scope 有限，auto-commit 需要額外設定
- `permissions:` 區塊可細粒度控制各項 scope
- `gh auth refresh -s workflow` 是補足權限的 CLI 方式
- 權限錯誤訊息要仔細看：`refusing to allow an OAuth App to create or update workflow` 表示缺 `workflow` scope

---

## GitHub Secrets 與 gh CLI 授權混淆
**日期**：2026-07-12
**環境**：GitHub Actions + Cloudflare Pages 部署

### 問題描述
設定自動部署時，誤以為「只要 gh CLI 授權完成就能部署」，忽略了 GitHub Secrets 的必要性。使用者問：「感覺重頭到尾都只是沒認證而已，GitHub Secrets 是必要的嗎？」

### 排查過程
1. gh CLI 授權完成後 `git push` 成功
2. 但 GitHub Actions workflow 仍需要 Cloudflare API Token 才能部署
3. 兩者作用不同：gh CLI 用於「推程式碼到 GitHub」，Secrets 用於「GitHub Actions 部署到 Cloudflare」

### 根因
對 CI/CD 流程的兩個階段理解混淆：
- **階段一**：本地 → GitHub（需要 gh CLI 授權）
- **階段二**：GitHub → Cloudflare（需要 GitHub Secrets）

### 解法
明確區分兩個認證：

| 認證 | 作用 | 設定位置 |
|------|------|----------|
| gh CLI OAuth | 允許 `git push` 到 GitHub | 本地 `gh auth refresh` |
| GitHub Secrets | 允許 Actions 呼叫 Cloudflare API | Repo Settings → Secrets |

Cloudflare Pages 部署需要的 Secrets：
- `CLOUDFLARE_API_TOKEN`：API Token（權限：Account → Cloudflare Pages → Edit）
- `CLOUDFLARE_ACCOUNT_ID`：32 字元 Account ID（從 Dashboard URL 取得）

### 教訓
- CI/CD 有兩個認證階段，不要混淆
- gh CLI 授權 ≠ GitHub Actions 部署權限
- Secrets 是 GitHub Actions 執行環境的變數，與本地 gh CLI 無關
- 刪除 Secrets 會導致部署失敗（即使 push 成功）

---
